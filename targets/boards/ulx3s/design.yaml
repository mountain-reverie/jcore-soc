# Plain `make ulx3s` (no VARIANT) uses the default j2-direct variant, which
# reproduces the historical hand-written one_cpu_m0_direct_fpga + idcache board.
# The board-level definition lives in base.yaml; CPU/cache topology in the
# variant files (design.<variant>.yaml).
<<: !include design.j2-direct.yaml
